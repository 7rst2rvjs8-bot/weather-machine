/* ============================================================
   STATE — the seam. Hand-edit these values; the site reads them.
   Rung 2: a local sanitizer emits exactly this object.
   Every field is wired into the DOM via [data-state="<field>"].
   ============================================================ */
window.STATE = {
  name: "THE WEATHER MACHINE",
  updated: "2026-06-22",        // YYYY-MM-DD
  baseline: "PROTECTED · BYTE-LOCKED",      // the live shadow bot
  firstEdge: "LIKELY WEATHER · NOT DURABLE",// the headline finding
  factory: "RUNNING · NON-PROMOTABLE",      // the research factory
  observer: "NOT YET BUILT",                // the next instrument
  universeCount: 9
};
